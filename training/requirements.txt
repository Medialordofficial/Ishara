# Ishara Training Environment
# GPU machine with ≥16 GB VRAM recommended (free Colab T4 works for gemma4:4b)

# Core training stack
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git
trl>=0.8.0
peft>=0.10.0
accelerate>=0.27.0
bitsandbytes>=0.42.0
datasets>=2.18.0
transformers>=4.40.0

# Optional: experiment tracking
# wandb

# Optional: push to HuggingFace
huggingface_hub>=0.22.0
